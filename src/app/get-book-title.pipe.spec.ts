import { GetBookTitlePipe } from './get-book-title.pipe';

describe('GetBookTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new GetBookTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
